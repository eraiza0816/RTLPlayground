name: Build and Release firmware

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        machine: [SWGT024_V2_0_MANAGED, PCB_K0402WS_V3]
    runs-on: ubuntu-latest
    container:
      image: debian:trixie-slim

    steps:
      - uses: actions/checkout@v7

      - name: Prepare SFP password dictionary stub
        run: |
          # sfp_pw_dict.inc is gitignored (local secret passwords). Create an
          # empty stub so the firmware builds with only the inline entries.
          touch sfp_pw_dict.inc

      - name: Install dependencies
        run: |
          apt update
          apt install make gcc sdcc xxd python-is-python3 libjson-c-dev git -y

      - name: Check if machine.c can be compiled for all machines
        run: make machine_check

      - name: Build firmware (WEB, Lite CLI)
        run: make MACHINE="${{ matrix.machine }}"

      - name: Run AEAD RFC 8439 vector test (firmware crypto, host build)
        run: ./tools/output/aead_test

      - name: Rename WEB Lite firmware
        run: |
          cd output/${{ matrix.machine }}
          for f in rtlplayground-v*-lite-*.bin; do
            mv "$f" "rtlplayground-WEB-${f#rtlplayground-}"
          done

      - name: Build firmware (CLI, no web, Lite CLI)
        run: |
          make MACHINE="${{ matrix.machine }}" clean
          make WEB=0 MACHINE="${{ matrix.machine }}"
          cd output/${{ matrix.machine }}
          for f in rtlplayground-v*-lite-*.bin; do
            mv "$f" "rtlplayground-CLI-${f#rtlplayground-}"
          done

      - name: Build firmware (WEB, Full CLI)
        run: |
          make MACHINE="${{ matrix.machine }}" clean
          make FULL=1 MACHINE="${{ matrix.machine }}"
          cd output/${{ matrix.machine }}
          for f in rtlplayground-v*-full-*.bin; do
            mv "$f" "rtlplayground-WEB-FULL-${f#rtlplayground-}"
          done

      - name: Build firmware (CLI, no web, Full CLI)
        run: |
          make MACHINE="${{ matrix.machine }}" clean
          make WEB=0 FULL=1 MACHINE="${{ matrix.machine }}"
          cd output/${{ matrix.machine }}
          for f in rtlplayground-v*-full-*.bin; do
            mv "$f" "rtlplayground-CLI-FULL-${f#rtlplayground-}"
          done

      - name: Upload firmware artifact
        uses: actions/upload-artifact@v7
        with:
          name: firmware-${{ matrix.machine }}
          path: output/${{ matrix.machine }}/*.bin

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v7

      - uses: actions/download-artifact@v8
        with:
          pattern: firmware-*
          merge-multiple: true

      - name: Extract version
        run: echo "FW_VERSION=v$(grep '^VERSION=' Makefile | cut -d= -f2)" >> $GITHUB_ENV

      - name: Create Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ env.FW_VERSION }}
          name: Firmware ${{ env.FW_VERSION }}
          body: |
            Automated firmware build for ${{ env.FW_VERSION }}.

            Includes builds for:
            - SWGT024_V2_0_MANAGED
            - PCB_K0402WS_V3
          files: "*.bin"
          make_latest: true
